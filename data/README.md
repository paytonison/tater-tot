# Data

Place plain UTF-8 or ASCII text files here for training.

For example:

```sh
cp ~/some_text.txt data/input.txt
```

The first implementation treats input as bytes/chars rather than Unicode code points.

